#amount

	Content:
	
	is a number. [property number]
	not a number.

	Format:

	two decimal places. 	[if number]
	not two decimal places. [if number]

	Value:

	amount >= price + 1. 	[if number]
	amount <= price + 1. 	[if number]
	
	Max amt:

	amount <= maxBid.	[if number]
